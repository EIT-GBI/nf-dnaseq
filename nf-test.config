config {
    testsDir "tests"
    workDir ".nf-test"
    configFile "nextflow.config"

    // Container-free by default so stub tests run anywhere. CI adds the
    // container engine with:  nf-test test --profile test,docker
    profile "test"
}
